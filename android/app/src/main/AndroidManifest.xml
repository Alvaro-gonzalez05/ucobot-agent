<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- El servicio en primer plano es lo que evita que Android duerma el agente:
         mientras haya una notificación fija, el sistema lo deja con red y no lo
         mata. Es el mismo mecanismo con el que Spotify sigue sonando con la
         pantalla apagada. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- La impresora integrada del iMin se expone como un dispositivo Bluetooth
         virtual llamado "InnerPrinter". No es Bluetooth de verdad (no hay radio
         de por medio): es cómo el fabricante publica la impresora al sistema. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Para actualizarse sola. Android igual exige que un humano confirme la
         instalación: esto sólo permite pedirla, no instalar en silencio. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.UcoBotAgent">

        <!-- UcoBot entero: la web adentro. configChanges evita que girar la
             pantalla o conectar un teclado recargue la página y pierda lo que el
             cajero estaba cargando. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Estado del equipo: impresora, timbre, versión y vinculación a mano. -->
        <activity
            android:name=".EstadoActivity"
            android:exported="false"
            android:label="Estado del equipo"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".AgentService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Le da al instalador del sistema permiso para leer el APK que bajamos.
             Sin esto Android bloquea el archivo y la actualización falla sin
             explicar por qué. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Que vuelva solo cuando se reinicia el equipo: en un local nadie se
             acuerda de abrir una app de fondo después de un corte de luz. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
